sudo: required

language: c

addons:
  apt:
    packages:
      - libreadline-dev
      - autoconf-archive
      - libgmp-dev
      - expect

before_script: autoreconf --install
script: ./configure --enable-debug && make && make check
